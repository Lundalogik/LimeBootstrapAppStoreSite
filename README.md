# LimeBootstrapAppStoreSite
A super vanilla, plain old static site to host the Lime Store.

### Important
Uses GitHub pages based on the master branch, i.e., everything done to the master branch goes into production!
